paths:
  /apiv6/Mod/ByName:
    get:
      operationId: v6_mod_by_name_get
      summary: Search resources by name (Mod)
      description: Use this endpoint to search resources by name (Mod). No parameters are required.
      parameters:
      - in: query
        name: _csvProperties
        required: false
        schema:
          type: string
        description: Optional query parameter `_csvProperties`. Comma-separated property whitelist. Restricts
          response payload to the listed fields where supported.
        example: _idRow,_sName
      - in: query
        name: _sName
        required: false
        schema:
          type: string
        description: Optional query parameter `_sName`. Name search string. Matched against endpoint-specific
          name/title fields.
        example: example
      - in: query
        name: _idGameRow
        required: false
        schema:
          type: integer
        description: Optional query parameter `_idGameRow`. Game row identifier. Targets operations to
          a specific game record.
        example: 8694
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv6/Mod/ByName
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  id: 1
                  name: example
                  attributes:
                    status: active
                errors: []
        '400':
          description: HTTP 400 response as observed for this endpoint.
        '401':
          description: HTTP 401 response as observed for this endpoint.
        '403':
          description: HTTP 403 response as observed for this endpoint.
        '404':
          description: HTTP 404 response as observed for this endpoint.
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v6: Mod'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv6/Mod/ByName?_csvProperties=_idRow%2C_sName%2C_aFiles%2C_aGame&_sName=%2Auwu%2A&_idGameRow=6498
        contentType: application/json; charset=utf-8
      x-request-example: GET https://gamebanana.com/apiv6/Mod/ByName
      x-observed-response-format: json
  /apiv6/Mod/Multi:
    get:
      operationId: v6_mod_multi_get
      summary: Fetch multiple resources (Mod)
      description: Use this endpoint to fetch multiple resources (Mod). No parameters are required.
      parameters:
      - in: query
        name: _csvProperties
        required: false
        schema:
          type: string
        description: Optional query parameter `_csvProperties`. Comma-separated property whitelist. Restricts
          response payload to the listed fields where supported.
        example: _idRow,_sName
      - in: query
        name: _csvRowIds
        required: false
        schema:
          type: integer
        description: Optional query parameter `_csvRowIds`. Comma-separated row identifiers. Batch-fetches
          specific rows in one request.
        example: 1,2,3
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv6/Mod/Multi
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  id: 1
                  name: example
                  attributes:
                    status: active
                errors: []
        '400':
          description: HTTP 400 response as observed for this endpoint.
        '401':
          description: HTTP 401 response as observed for this endpoint.
        '403':
          description: HTTP 403 response as observed for this endpoint.
        '404':
          description: HTTP 404 response as observed for this endpoint.
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v6: Mod'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv6/Mod/Multi?_csvProperties=_aModManagerIntegrations%2C_sName%2C_bHasUpdates%2C_aLatestUpdates%2C_aFiles%2C_aPreviewMedia%2C_aAlternateFileSources&_csvRowIds=9651%2C50891%2C50891%2C34011%2C34011%2C50935%2C250364%2C50932%2C250391%2C285496%2C50912%2C50934%2C50934%2C50910%2C50882%2C50882%2C50882%2C9647%2C50895%2C50893%2C50894%2C50903%2C50871%2C50930%2C33986%2C50931%2C9657%2C138058%2C50885%2C50885%2C50885%2C50885%2C33994%2C33990%2C293809%2C50908
        contentType: application/json; charset=utf-8
      x-request-example: GET https://gamebanana.com/apiv6/Mod/Multi
      x-observed-response-format: json
