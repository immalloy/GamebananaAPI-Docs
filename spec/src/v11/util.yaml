paths:
  /apiv11/Util/Config/TrashReasons:
    get:
      operationId: v11_util_config_trash_reasons_get
      summary: List trash reasons (Util)
      description: Use this endpoint to list trash reasons (Util). No parameters are required.
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Util/Config/TrashReasons
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  items:
                  - id: 1
                    name: trashreasons-example-1
                  - id: 2
                    name: trashreasons-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Util'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Util/Config/TrashReasons
        contentType: application/json; charset=UTF-8
      x-request-example: GET https://gamebanana.com/apiv11/Util/Config/TrashReasons
      x-observed-response-format: json
  /apiv11/Util/Game/NameMatch:
    get:
      operationId: v11_util_game_name_match_get
      summary: Run game name match (Util)
      description: Use this endpoint to run game name match (Util). Required path and query values are
        listed below.
      parameters:
      - in: query
        name: _sName
        required: true
        schema:
          type: string
        description: Required query parameter `_sName`. Name search string. Matched against endpoint-specific
          name/title fields.
        example: example
      - in: query
        name: _nPage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPage`. 1-based page index for paginated results. Use
          `1` for the first page and increment for subsequent pages.
        example: 1
      - in: query
        name: _nPerpage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPerpage`. Maximum items to return per page. Tune this
          for payload size vs. request count tradeoffs.
        example: 15
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Util/Game/NameMatch
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  id: 1
                  name: example
                  attributes:
                    status: active
                errors: []
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Util'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Util/Game/NameMatch?_sName=test
        contentType: application/json; charset=UTF-8
      x-request-example: GET https://gamebanana.com/apiv11/Util/Game/NameMatch
      x-observed-response-format: json
  /apiv11/Util/Generic/Discussions:
    get:
      operationId: v11_util_generic_discussions_get
      summary: List generic discussions (Util)
      description: Use this endpoint to list generic discussions (Util). No parameters are required.
      parameters:
      - in: query
        name: _idGameRow
        required: false
        schema:
          type: integer
        description: Optional query parameter `_idGameRow`. Game row identifier. Targets operations to
          a specific game record.
        example: 8694
      - in: query
        name: _idMemberRow
        required: false
        schema:
          type: integer
        description: Optional query parameter `_idMemberRow`. Member row identifier. Targets operations
          to a specific member record.
        example: 1
      - in: query
        name: _nPage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPage`. 1-based page index for paginated results. Use
          `1` for the first page and increment for subsequent pages.
        example: 1
      - in: query
        name: _nPerpage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPerpage`. Maximum items to return per page. Tune this
          for payload size vs. request count tradeoffs.
        example: 15
      - in: query
        name: _sModelName
        required: false
        schema:
          type: string
        description: Optional query parameter `_sModelName`. Model/domain name selector. Used by generic
          endpoints to choose the target model.
        example: Mod
      responses:
        '0':
          description: HTTP 0 response as observed for this endpoint.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv11/Util/Generic/Discussions`. Field presence can
                  vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv11/Util/Generic/Discussions
                  method: GET
                  status: '0'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  items:
                  - id: 1
                    name: discussions-example-1
                  - id: 2
                    name: discussions-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Util/Generic/Discussions
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  items:
                  - id: 1
                    name: discussions-example-1
                  - id: 2
                    name: discussions-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Util'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Util/Generic/Discussions
        contentType: application/json; charset=UTF-8
      x-request-example: GET https://gamebanana.com/apiv11/Util/Generic/Discussions
      x-observed-response-format: json
  /apiv11/Util/Generic/Tags:
    get:
      operationId: v11_util_generic_tags_get
      summary: List generic tags (Util)
      description: Use this endpoint to list generic tags (Util). Required path and query values are listed
        below.
      parameters:
      - in: query
        name: _sTag
        required: true
        schema:
          type: string
        description: Required query parameter `_sTag`. Tag filter value. Limits results to entities associated
          with this tag.
        example: featured
      - in: query
        name: _idGameRow
        required: false
        schema:
          type: integer
        description: Optional query parameter `_idGameRow`. Game row identifier. Targets operations to
          a specific game record.
        example: 8694
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Util/Generic/Tags
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  items:
                  - id: 1
                    name: tags-example-1
                  - id: 2
                    name: tags-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Util'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Util/Generic/Tags?_sTag=test
        contentType: application/json; charset=UTF-8
      x-request-example: GET https://gamebanana.com/apiv11/Util/Generic/Tags
      x-observed-response-format: json
  /apiv11/Util/Homepage/CommunitySpotlight:
    get:
      operationId: v11_util_homepage_community_spotlight_get
      summary: Get community spotlight content (Util)
      description: Use this endpoint to get community spotlight content (Util). No parameters are required.
        This endpoint may return HTML, so check the response content type before parsing.
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Util/Homepage/CommunitySpotlight
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  id: 1
                  name: example
                  attributes:
                    status: active
                errors: []
            text/html:
              schema:
                type: string
                description: Raw response body.
              example: <!doctype html><html><body>Observed HTML payload from GameBanana endpoint.</body></html>
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Util'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Util/Homepage/CommunitySpotlight
        contentType: text/html; charset=UTF-8
      x-curation-note: 'uncertain: probe content-type is not JSON'
      x-request-example: GET https://gamebanana.com/apiv11/Util/Homepage/CommunitySpotlight
      x-observed-response-format: mixed
      x-observed-response-note: 'Observed format drift: previous docs had `html`, full live verification
        on 2026-06-01T21:22:07Z returned `json` (application/json; charset=UTF-8).'
  /apiv11/Util/Homepage/Features:
    get:
      operationId: v11_util_homepage_features_get
      summary: List homepage features (Util)
      description: Use this endpoint to list homepage features (Util). No parameters are required. This
        endpoint may return HTML, so check the response content type before parsing.
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Util/Homepage/Features
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  items:
                  - id: 1
                    name: features-example-1
                  - id: 2
                    name: features-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
            text/html:
              schema:
                type: string
                description: Raw response body.
              example: <!doctype html><html><body>Observed HTML payload from GameBanana endpoint.</body></html>
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Util'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Util/Homepage/Features
        contentType: text/html; charset=UTF-8
      x-curation-note: 'uncertain: probe content-type is not JSON'
      x-request-example: GET https://gamebanana.com/apiv11/Util/Homepage/Features
      x-observed-response-format: mixed
      x-observed-response-note: 'Observed format drift: previous docs had `html`, full live verification
        on 2026-06-01T21:22:07Z returned `json` (application/json; charset=UTF-8).'
  /apiv11/Util/Homepage/Submissions:
    get:
      operationId: v11_util_homepage_submissions_get
      summary: List submissions (Util)
      description: Use this endpoint to list submissions (Util). No parameters are required.
      parameters:
      - in: query
        name: _bFilterBySubscribedGames
        required: false
        schema:
          type: boolean
        description: Optional query parameter `_bFilterBySubscribedGames`. Endpoint-specific query parameter.
          Use only values accepted by this route; unsupported values may be ignored.
        example: example
      - in: query
        name: _nPage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPage`. 1-based page index for paginated results. Use
          `1` for the first page and increment for subsequent pages.
        example: 1
      - in: query
        name: _nPerpage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPerpage`. Maximum items to return per page. Tune this
          for payload size vs. request count tradeoffs.
        example: 15
      - in: query
        name: _sSort
        required: false
        schema:
          type: string
        description: Optional query parameter `_sSort`. Sort mode key used by the endpoint. Use server-supported
          values from `ListFilterConfig` where available.
        example: newest
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Util/Homepage/Submissions
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  items:
                  - id: 1
                    name: submissions-example-1
                  - id: 2
                    name: submissions-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Util'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Util/Homepage/Submissions
        contentType: application/json; charset=UTF-8
      x-request-example: GET https://gamebanana.com/apiv11/Util/Homepage/Submissions
      x-observed-response-format: json
  /apiv11/Util/Homepage/TopGames:
    get:
      operationId: v11_util_homepage_top_games_get
      summary: List top games (Util)
      description: Use this endpoint to list top games (Util). No parameters are required. This endpoint
        may return HTML, so check the response content type before parsing.
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Util/Homepage/TopGames
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  items:
                  - id: 1
                    name: topgames-example-1
                  - id: 2
                    name: topgames-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
            text/html:
              schema:
                type: string
                description: Raw response body.
              example: <!doctype html><html><body>Observed HTML payload from GameBanana endpoint.</body></html>
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Util'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Util/Homepage/TopGames
        contentType: text/html; charset=UTF-8
      x-curation-note: 'uncertain: probe content-type is not JSON'
      x-request-example: GET https://gamebanana.com/apiv11/Util/Homepage/TopGames
      x-observed-response-format: mixed
      x-observed-response-note: 'Observed format drift: previous docs had `html`, full live verification
        on 2026-06-01T21:22:07Z returned `json` (application/json; charset=UTF-8).'
  /apiv11/Util/RecordInfo:
    get:
      operationId: v11_util_record_info_get
      summary: Get record info (Util)
      description: Use this endpoint to get record info (Util). No parameters are required.
      parameters:
      - in: query
        name: s
        required: false
        schema:
          type: string
        description: Optional query parameter `s`. Endpoint-specific query parameter. Use only values
          accepted by this route; unsupported values may be ignored.
        example: example
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Util/RecordInfo
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  id: 1
                  name: example
                  attributes:
                    status: active
                errors: []
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Util'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Util/RecordInfo
        contentType: application/json; charset=UTF-8
      x-request-example: GET https://gamebanana.com/apiv11/Util/RecordInfo
      x-observed-response-format: json
