paths:
  /apiv11/{section_slug}/{submission_id}/Collections:
    get:
      operationId: v11_sectionslug_submission_id_collections_get
      summary: List submission collections ({section_slug})
      description: 'v11 `{section_slug}` endpoint. Purpose: Return collections that
        include the target submission. Expected input: Provide required path identifiers
        plus optional query filters where supported. Returned data: JSON payload used
        by GameBanana clients for list, detail, or interaction views. Last verified
        probe content-type: `application/json; charset=UTF-8`.'
      parameters:
      - in: path
        name: section_slug
        required: true
        schema:
          type: string
        description: Required path parameter `section_slug`. Section slug identifying
          the submission domain (for example `Mod`, `Tool`, `Sound`). Choose a slug
          that maps to a supported GameBanana section.
        example: Mod
      - in: path
        name: submission_id
        required: true
        schema:
          type: integer
        description: Required path parameter `submission_id`. Numeric identifier for
          the target entity. Use an ID from index/search endpoints or previously resolved
          links.
        example: 1
      - in: query
        name: _nPage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPage`. 1-based page index for paginated
          results. Use `1` for the first page and increment for subsequent pages.
        example: 1
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv11/{section_slug}/{submission_id}/Collections`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv11/{section_slug}/{submission_id}/Collections
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  items:
                  - id: 1
                    name: collections-example-1
                  - id: 2
                    name: collections-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v11: {section_slug}'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/Collections
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `Collections` responses to drive list views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv11/example/1/Collections
      x-observed-response-format: json
  /apiv11/{section_slug}/{submission_id}/DownloadPage:
    get:
      operationId: v11_sectionslug_submission_id_download_page_get
      summary: Get download page payload ({section_slug})
      description: 'v11 `{section_slug}` endpoint. Purpose: Return download-page metadata
        and assets for the target submission. Expected input: Provide required path
        identifiers plus optional query filters where supported. Returned data: JSON
        payload used by GameBanana clients for list, detail, or interaction views.
        Last verified probe content-type: `application/json; charset=UTF-8`.'
      parameters:
      - in: path
        name: section_slug
        required: true
        schema:
          type: string
        description: Required path parameter `section_slug`. Section slug identifying
          the submission domain (for example `Mod`, `Tool`, `Sound`). Choose a slug
          that maps to a supported GameBanana section.
        example: Mod
      - in: path
        name: submission_id
        required: true
        schema:
          type: integer
        description: Required path parameter `submission_id`. Numeric identifier for
          the target entity. Use an ID from index/search endpoints or previously resolved
          links.
        example: 1
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv11/{section_slug}/{submission_id}/DownloadPage`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv11/{section_slug}/{submission_id}/DownloadPage
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  download:
                    url: https://gamebanana.com/dl/example
                    filename: example.zip
                  checksum:
                    sha256: <hash>
                errors: []
            text/html:
              schema:
                type: string
                description: HTML response payload observed in live verification.
              example: <!doctype html><html><body>Observed HTML payload.</body></html>
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v11: {section_slug}'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/DownloadPage
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `DownloadPage` responses to drive download views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv11/example/1/DownloadPage
      x-observed-response-format: mixed
      x-observed-response-note: 'Observed format drift: previous docs had `json`,
        full live verification on 2026-06-01T21:22:07Z returned `html` (text/html;
        charset=UTF-8).'
  /apiv11/{section_slug}/{submission_id}/EmbeddablesPage:
    get:
      operationId: v11_sectionslug_submission_id_embeddables_page_get
      summary: Get embeddables page payload ({section_slug})
      description: 'v11 `{section_slug}` endpoint. Purpose: Return embed-ready content
        and metadata for the submission. Expected input: Provide required path identifiers
        plus optional query filters where supported. Returned data: JSON payload used
        by GameBanana clients for list, detail, or interaction views. Last verified
        probe content-type: `application/json; charset=UTF-8`.'
      parameters:
      - in: path
        name: section_slug
        required: true
        schema:
          type: string
        description: Required path parameter `section_slug`. Section slug identifying
          the submission domain (for example `Mod`, `Tool`, `Sound`). Choose a slug
          that maps to a supported GameBanana section.
        example: Mod
      - in: path
        name: submission_id
        required: true
        schema:
          type: integer
        description: Required path parameter `submission_id`. Numeric identifier for
          the target entity. Use an ID from index/search endpoints or previously resolved
          links.
        example: 1
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv11/{section_slug}/{submission_id}/EmbeddablesPage`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv11/{section_slug}/{submission_id}/EmbeddablesPage
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  id: 1
                  name: example
                  attributes:
                    status: active
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v11: {section_slug}'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/EmbeddablesPage
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `EmbeddablesPage` responses to drive detail views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv11/example/1/EmbeddablesPage
      x-observed-response-format: json
  /apiv11/{section_slug}/{submission_id}/Files:
    get:
      operationId: v11_sectionslug_submission_id_files_get
      summary: List submission files ({section_slug})
      description: 'v11 `{section_slug}` endpoint. Purpose: Return files attached
        to the target submission. Expected input: Provide required path identifiers
        plus optional query filters where supported. Returned data: JSON payload used
        by GameBanana clients for list, detail, or interaction views. Last verified
        probe content-type: `application/json; charset=UTF-8`.'
      parameters:
      - in: path
        name: section_slug
        required: true
        schema:
          type: string
        description: Required path parameter `section_slug`. Section slug identifying
          the submission domain (for example `Mod`, `Tool`, `Sound`). Choose a slug
          that maps to a supported GameBanana section.
        example: Mod
      - in: path
        name: submission_id
        required: true
        schema:
          type: integer
        description: Required path parameter `submission_id`. Numeric identifier for
          the target entity. Use an ID from index/search endpoints or previously resolved
          links.
        example: 1
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv11/{section_slug}/{submission_id}/Files`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv11/{section_slug}/{submission_id}/Files
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  items:
                  - id: 1
                    name: files-example-1
                  - id: 2
                    name: files-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v11: {section_slug}'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/Files
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `Files` responses to drive list views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv11/example/1/Files
      x-observed-response-format: json
  /apiv11/{section_slug}/{submission_id}/Likes:
    get:
      operationId: v11_sectionslug_submission_id_likes_get
      summary: List submission likes ({section_slug})
      description: 'v11 `{section_slug}` endpoint. Purpose: Return like interactions
        for the target submission. Expected input: Provide required path identifiers
        plus optional query filters where supported. Returned data: JSON payload used
        by GameBanana clients for list, detail, or interaction views. Last verified
        probe content-type: `application/json; charset=UTF-8`.'
      parameters:
      - in: path
        name: section_slug
        required: true
        schema:
          type: string
        description: Required path parameter `section_slug`. Section slug identifying
          the submission domain (for example `Mod`, `Tool`, `Sound`). Choose a slug
          that maps to a supported GameBanana section.
        example: Mod
      - in: path
        name: submission_id
        required: true
        schema:
          type: integer
        description: Required path parameter `submission_id`. Numeric identifier for
          the target entity. Use an ID from index/search endpoints or previously resolved
          links.
        example: 1
      - in: query
        name: _nPage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPage`. 1-based page index for paginated
          results. Use `1` for the first page and increment for subsequent pages.
        example: 1
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv11/{section_slug}/{submission_id}/Likes`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv11/{section_slug}/{submission_id}/Likes
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  items:
                  - id: 1
                    name: likes-example-1
                  - id: 2
                    name: likes-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v11: {section_slug}'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/Likes
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `Likes` responses to drive list views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv11/example/1/Likes
      x-observed-response-format: json
  /apiv11/{section_slug}/{submission_id}/Posts:
    get:
      operationId: v11_sectionslug_submission_id_posts_get
      summary: List discussion posts ({section_slug})
      description: 'v11 `{section_slug}` endpoint `{submission Id} / Posts`. Purpose:
        Return post records tied to the resource, often for comments or activity feeds.
        Expected input: Provide required path identifiers and optional query filters
        as documented below. Returned data: typically a domain-specific JSON object/collection
        used by GameBanana web clients and integrations. Last verified probe content-type:
        `application/json; charset=UTF-8`.'
      parameters:
      - in: path
        name: section_slug
        required: true
        schema:
          type: string
        description: Required path parameter `section_slug`. Section slug identifying
          the submission domain (for example `Mod`, `Tool`, `Sound`). Choose a slug
          that maps to a supported GameBanana section.
        example: Mod
      - in: path
        name: submission_id
        required: true
        schema:
          type: integer
        description: Required path parameter `submission_id`. Numeric identifier for
          the target entity. Use an ID from index/search endpoints or previously resolved
          links.
        example: 1
      - in: query
        name: _nPage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPage`. 1-based page index for paginated
          results. Use `1` for the first page and increment for subsequent pages.
        example: 1
      - in: query
        name: _nPerpage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPerpage`. Maximum items to return
          per page. Tune this for payload size vs. request count tradeoffs.
        example: 15
      - in: query
        name: _sSort
        required: false
        schema:
          type: string
        description: Optional query parameter `_sSort`. Sort mode key used by the
          endpoint. Use server-supported values from `ListFilterConfig` where available.
        example: newest
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv11/{section_slug}/{submission_id}/Posts`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv11/{section_slug}/{submission_id}/Posts
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  items:
                  - id: 1
                    name: posts-example-1
                  - id: 2
                    name: posts-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v11: {section_slug}'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/Posts
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `Posts` responses to drive list views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv11/example/1/Posts
      x-observed-response-format: json
  /apiv11/{section_slug}/{submission_id}/ProfilePage:
    get:
      operationId: v11_sectionslug_submission_id_profile_page_get
      summary: Fetch profile page payload ({section_slug})
      description: 'v11 `{section_slug}` endpoint `{submission Id} / Profile Page`.
        Purpose: Return the full profile-page data model used by GameBanana clients
        for a single entity. Expected input: Provide required path identifiers and
        optional query filters as documented below. Returned data: typically a domain-specific
        JSON object/collection used by GameBanana web clients and integrations. Last
        verified probe content-type: `application/json; charset=UTF-8`.'
      parameters:
      - in: path
        name: section_slug
        required: true
        schema:
          type: string
        description: Required path parameter `section_slug`. Section slug identifying
          the submission domain (for example `Mod`, `Tool`, `Sound`). Choose a slug
          that maps to a supported GameBanana section.
        example: Mod
      - in: path
        name: submission_id
        required: true
        schema:
          type: integer
        description: Required path parameter `submission_id`. Numeric identifier for
          the target entity. Use an ID from index/search endpoints or previously resolved
          links.
        example: 1
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv11/{section_slug}/{submission_id}/ProfilePage`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv11/{section_slug}/{submission_id}/ProfilePage
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  profile:
                    id: 1
                    name: Example
                    summary: Example profile payload
                  stats:
                    views: 123
                    likes: 10
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v11: {section_slug}'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/ProfilePage
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `ProfilePage` responses to drive profile views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv11/example/1/ProfilePage
      x-observed-response-format: json
  /apiv11/{section_slug}/{submission_id}/Subscribers:
    get:
      operationId: v11_sectionslug_submission_id_subscribers_get
      summary: List submission subscribers ({section_slug})
      description: 'v11 `{section_slug}` endpoint. Purpose: Return members subscribed
        to the target submission. Expected input: Provide required path identifiers
        plus optional query filters where supported. Returned data: JSON payload used
        by GameBanana clients for list, detail, or interaction views. Last verified
        probe content-type: `application/json; charset=UTF-8`.'
      parameters:
      - in: path
        name: section_slug
        required: true
        schema:
          type: string
        description: Required path parameter `section_slug`. Section slug identifying
          the submission domain (for example `Mod`, `Tool`, `Sound`). Choose a slug
          that maps to a supported GameBanana section.
        example: Mod
      - in: path
        name: submission_id
        required: true
        schema:
          type: integer
        description: Required path parameter `submission_id`. Numeric identifier for
          the target entity. Use an ID from index/search endpoints or previously resolved
          links.
        example: 1
      - in: query
        name: _nPage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPage`. 1-based page index for paginated
          results. Use `1` for the first page and increment for subsequent pages.
        example: 1
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv11/{section_slug}/{submission_id}/Subscribers`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv11/{section_slug}/{submission_id}/Subscribers
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  items:
                  - id: 1
                    name: subscribers-example-1
                  - id: 2
                    name: subscribers-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v11: {section_slug}'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/Subscribers
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `Subscribers` responses to drive list views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv11/example/1/Subscribers
      x-observed-response-format: json
  /apiv11/{section_slug}/{submission_id}/Thanks:
    get:
      operationId: v11_sectionslug_submission_id_thanks_get
      summary: List submission thanks ({section_slug})
      description: 'v11 `{section_slug}` endpoint. Purpose: Return member thanks/acknowledgements
        for the target submission. Expected input: Provide required path identifiers
        plus optional query filters where supported. Returned data: JSON payload used
        by GameBanana clients for list, detail, or interaction views. Last verified
        probe content-type: `application/json; charset=UTF-8`.'
      parameters:
      - in: path
        name: section_slug
        required: true
        schema:
          type: string
        description: Required path parameter `section_slug`. Section slug identifying
          the submission domain (for example `Mod`, `Tool`, `Sound`). Choose a slug
          that maps to a supported GameBanana section.
        example: Mod
      - in: path
        name: submission_id
        required: true
        schema:
          type: integer
        description: Required path parameter `submission_id`. Numeric identifier for
          the target entity. Use an ID from index/search endpoints or previously resolved
          links.
        example: 1
      - in: query
        name: _nPage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPage`. 1-based page index for paginated
          results. Use `1` for the first page and increment for subsequent pages.
        example: 1
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv11/{section_slug}/{submission_id}/Thanks`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv11/{section_slug}/{submission_id}/Thanks
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  items:
                  - id: 1
                    name: thanks-example-1
                  - id: 2
                    name: thanks-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v11: {section_slug}'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/Thanks
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `Thanks` responses to drive list views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv11/example/1/Thanks
      x-observed-response-format: json
  /apiv11/{section_slug}/{submission_id}/Todos:
    get:
      operationId: v11_sectionslug_submission_id_todos_get
      summary: List submission todos ({section_slug})
      description: 'v11 `{section_slug}` endpoint. Purpose: Return TODO entries associated
        with the target submission. Expected input: Provide required path identifiers
        plus optional query filters where supported. Returned data: JSON payload used
        by GameBanana clients for list, detail, or interaction views. Last verified
        probe content-type: `application/json; charset=UTF-8`.'
      parameters:
      - in: path
        name: section_slug
        required: true
        schema:
          type: string
        description: Required path parameter `section_slug`. Section slug identifying
          the submission domain (for example `Mod`, `Tool`, `Sound`). Choose a slug
          that maps to a supported GameBanana section.
        example: Mod
      - in: path
        name: submission_id
        required: true
        schema:
          type: integer
        description: Required path parameter `submission_id`. Numeric identifier for
          the target entity. Use an ID from index/search endpoints or previously resolved
          links.
        example: 1
      - in: query
        name: _nPage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPage`. 1-based page index for paginated
          results. Use `1` for the first page and increment for subsequent pages.
        example: 1
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv11/{section_slug}/{submission_id}/Todos`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv11/{section_slug}/{submission_id}/Todos
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  items:
                  - id: 1
                    name: todos-example-1
                  - id: 2
                    name: todos-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v11: {section_slug}'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/Todos
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `Todos` responses to drive list views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv11/example/1/Todos
      x-observed-response-format: json
  /apiv11/{section_slug}/{submission_id}/Updates:
    get:
      operationId: v11_sectionslug_submission_id_updates_get
      summary: List submission updates ({section_slug})
      description: 'v11 `{section_slug}` endpoint. Purpose: Return update/change-log
        entries for the target submission. Expected input: Provide required path identifiers
        plus optional query filters where supported. Returned data: JSON payload used
        by GameBanana clients for list, detail, or interaction views. Last verified
        probe content-type: `application/json; charset=UTF-8`.'
      parameters:
      - in: path
        name: section_slug
        required: true
        schema:
          type: string
        description: Required path parameter `section_slug`. Section slug identifying
          the submission domain (for example `Mod`, `Tool`, `Sound`). Choose a slug
          that maps to a supported GameBanana section.
        example: Mod
      - in: path
        name: submission_id
        required: true
        schema:
          type: integer
        description: Required path parameter `submission_id`. Numeric identifier for
          the target entity. Use an ID from index/search endpoints or previously resolved
          links.
        example: 1
      - in: query
        name: _nPage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPage`. 1-based page index for paginated
          results. Use `1` for the first page and increment for subsequent pages.
        example: 1
      - in: query
        name: _nPerpage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPerpage`. Maximum items to return
          per page. Tune this for payload size vs. request count tradeoffs.
        example: 15
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv11/{section_slug}/{submission_id}/Updates`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv11/{section_slug}/{submission_id}/Updates
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  items:
                  - id: 1
                    name: updates-example-1
                  - id: 2
                    name: updates-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v11: {section_slug}'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/Updates
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `Updates` responses to drive list views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv11/example/1/Updates
      x-observed-response-format: json
