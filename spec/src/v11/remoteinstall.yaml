paths:
  /apiv11/RemoteInstall/{memberid}/{secretkey}/PizzaOvenPlus:
    get:
      operationId: v11_remoteinstall_memberid_secretkey_pizza_oven_plus_get
      summary: Get PizzaOvenPlus install payload (RemoteInstall)
      description: Use this endpoint to get PizzaOvenPlus install payload (RemoteInstall). Required path
        and query values are listed below.
      parameters:
      - in: path
        name: memberid
        required: true
        schema:
          type: integer
        description: ID used in the `memberid` URL segment.
        example: 1
      - in: path
        name: secretkey
        required: true
        schema:
          type: string
        description: ID used in the `secretkey` URL segment.
        example: <secret>
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/RemoteInstall/{memberid}/{secretkey}/PizzaOvenPlus
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  download:
                    url: https://gamebanana.com/dl/example
                    filename: example.zip
                  checksum:
                    sha256: <hash>
                errors: []
        '400':
          description: HTTP 400 response as observed for this endpoint.
        '401':
          description: HTTP 401 response as observed for this endpoint.
        '403':
          description: HTTP 403 response as observed for this endpoint.
        '404':
          description: HTTP 404 response as observed for this endpoint.
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: RemoteInstall'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/RemoteInstall/1/test/PizzaOvenPlus
        contentType: application/json; charset=UTF-8
      x-request-example: GET https://gamebanana.com/apiv11/RemoteInstall/1/example/PizzaOvenPlus
      x-observed-response-format: json
