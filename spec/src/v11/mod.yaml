paths:
  /apiv11/Mod/Index:
    get:
      operationId: v11_mod_index_get
      summary: List primary resources (Mod)
      description: Use this endpoint to list primary resources (Mod). No parameters are required.
      parameters:
      - in: query
        name: _aFilters[Generic_Category]
        required: false
        schema:
          type: integer
        description: Optional query parameter `_aFilters[Generic_Category]`. Filter key `Generic_Category`
          used by server-side query filtering. Combine with pagination/sort parameters to build faceted
          result queries.
        example: example
      - in: query
        name: _aFilters[Generic_Game]
        required: false
        schema:
          type: integer
        description: Optional query parameter `_aFilters[Generic_Game]`. Filter key `Generic_Game` used
          by server-side query filtering. Combine with pagination/sort parameters to build faceted result
          queries.
        example: example
      - in: query
        name: _aFilters[Generic_Studio]
        required: false
        schema:
          type: integer
        description: Optional query parameter `_aFilters[Generic_Studio]`. Filter key `Generic_Studio`
          used by server-side query filtering. Combine with pagination/sort parameters to build faceted
          result queries.
        example: example
      - in: query
        name: _aFilters[Generic_Submitter]
        required: false
        schema:
          type: integer
        description: Optional query parameter `_aFilters[Generic_Submitter]`. Filter key `Generic_Submitter`
          used by server-side query filtering. Combine with pagination/sort parameters to build faceted
          result queries.
        example: example
      - in: query
        name: _nPage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPage`. 1-based page index for paginated results. Use
          `1` for the first page and increment for subsequent pages.
        example: 1
      - in: query
        name: _nPerpage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPerpage`. Maximum items to return per page. Tune this
          for payload size vs. request count tradeoffs.
        example: 15
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Mod/Index
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  items:
                  - id: 1
                    name: index-example-1
                  - id: 2
                    name: index-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Mod'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/Index
        contentType: application/json; charset=UTF-8
      x-request-example: GET https://gamebanana.com/apiv11/Mod/Index
      x-observed-response-format: json
  /apiv11/Mod/ListFilterConfig:
    get:
      operationId: v11_mod_list_filter_config_get
      summary: Get list filter configuration (Mod)
      description: Use this endpoint to get list filter configuration (Mod). No parameters are required.
        This endpoint may return HTML, so check the response content type before parsing.
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Mod/ListFilterConfig
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  items:
                  - id: 1
                    name: listfilterconfig-example-1
                  - id: 2
                    name: listfilterconfig-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
            text/html:
              schema:
                type: string
                description: Raw response body.
              example: <!doctype html><html><body>Observed HTML payload from GameBanana endpoint.</body></html>
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Mod'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/ListFilterConfig
        contentType: text/html; charset=UTF-8
      x-curation-note: 'uncertain: probe content-type is not JSON'
      x-request-example: GET https://gamebanana.com/apiv11/Mod/ListFilterConfig
      x-observed-response-format: html
  /apiv11/Mod/Multi:
    get:
      operationId: v11_mod_multi_get
      summary: Fetch multiple resources (Mod)
      description: Use this endpoint to fetch multiple resources (Mod). No parameters are required.
      parameters:
      - in: query
        name: _csvRowIds
        required: false
        schema:
          type: integer
        description: Optional query parameter `_csvRowIds`. Comma-separated row identifiers. Batch-fetches
          specific rows in one request.
        example: 1,2,3
      - in: query
        name: _csvProperties
        required: false
        schema:
          type: string
        description: Optional query parameter `_csvProperties`. Comma-separated property whitelist. Restricts
          response payload to the listed fields where supported.
        example: _idRow,_sName
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Mod/Multi
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  id: 1
                  name: example
                  attributes:
                    status: active
                errors: []
        '400':
          description: HTTP 400 response as observed for this endpoint.
        '401':
          description: HTTP 401 response as observed for this endpoint.
        '403':
          description: HTTP 403 response as observed for this endpoint.
        '404':
          description: HTTP 404 response as observed for this endpoint.
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Mod'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/Multi?_csvRowIds=506678%2C547865%2C547860%2C490773%2C34283&_csvProperties=_idRow%2C_sName%2C_aSubmitter%2C_tsDateAdded%2C_sVersion%2C_aRootCategory%2C_aPreviewMedia%2C_nLikeCount%2C_nViewCount%2C_aFiles%2C_sDescription%2C_sText%2C_nDownloadCount%2C_aModManagerIntegrations
        contentType: application/json; charset=UTF-8
      x-request-example: GET https://gamebanana.com/apiv11/Mod/Multi
      x-observed-response-format: json
  /apiv11/Mod/{id1}/Config:
    get:
      operationId: v11_mod_id1_config_get
      summary: Get resource configuration (Mod)
      description: Use this endpoint to get resource configuration (Mod). Required path and query values
        are listed below.
      parameters:
      - in: path
        name: id1
        required: true
        schema:
          type: integer
        description: ID used in the `id1` URL segment.
        example: 1
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Mod/{id1}/Config
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  filters:
                  - key: sort
                    values:
                    - newest
                    - popular
                  defaults:
                    sort: newest
                errors: []
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Mod'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/Config
        contentType: application/json; charset=UTF-8
      x-request-example: GET https://gamebanana.com/apiv11/Mod/1/Config
      x-observed-response-format: json
  /apiv11/Mod/{id1}/Posts:
    get:
      operationId: v11_mod_id1_posts_get
      summary: List discussion posts (Mod)
      description: Use this endpoint to list discussion posts (Mod). Required path and query values are
        listed below.
      parameters:
      - in: path
        name: id1
        required: true
        schema:
          type: integer
        description: ID used in the `id1` URL segment.
        example: 1
      - in: query
        name: _nPage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPage`. 1-based page index for paginated results. Use
          `1` for the first page and increment for subsequent pages.
        example: 1
      - in: query
        name: _nPerpage
        required: false
        schema:
          type: integer
        description: Optional query parameter `_nPerpage`. Maximum items to return per page. Tune this
          for payload size vs. request count tradeoffs.
        example: 15
      - in: query
        name: _sSort
        required: false
        schema:
          type: string
        description: Optional query parameter `_sSort`. Sort mode key used by the endpoint. Use server-supported
          values from `ListFilterConfig` where available.
        example: newest
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Mod/{id1}/Posts
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  items:
                  - id: 1
                    name: posts-example-1
                  - id: 2
                    name: posts-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Mod'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/Posts
        contentType: application/json; charset=UTF-8
      x-request-example: GET https://gamebanana.com/apiv11/Mod/1/Posts
      x-observed-response-format: json
  /apiv11/Mod/{id1}/ProfilePage:
    get:
      operationId: v11_mod_id1_profile_page_get
      summary: Fetch profile page payload (Mod)
      description: Use this endpoint to fetch profile page payload (Mod). Required path and query values
        are listed below.
      parameters:
      - in: path
        name: id1
        required: true
        schema:
          type: integer
        description: ID used in the `id1` URL segment.
        example: 1
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Mod/{id1}/ProfilePage
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  profile:
                    id: 1
                    name: Example
                    summary: Example profile payload
                  stats:
                    views: 123
                    likes: 10
                errors: []
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Mod'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/ProfilePage
        contentType: application/json; charset=UTF-8
      x-request-example: GET https://gamebanana.com/apiv11/Mod/1/ProfilePage
      x-observed-response-format: json
  /apiv11/Mod/{mod_id}/DownloadPage:
    get:
      operationId: v11_mod_mod_id_download_page_get
      summary: Get download page payload (Mod)
      description: Use this endpoint to get download page payload (Mod). Required path and query values
        are listed below. This endpoint may return HTML, so check the response content type before parsing.
      parameters:
      - in: path
        name: mod_id
        required: true
        schema:
          type: integer
        description: ID used in the `mod_id` URL segment.
        example: 1
      responses:
        '200':
          description: Successful response. Shape can vary by resource and permissions.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response body. Fields can vary by endpoint, resource state, and permissions.
              example:
                _meta:
                  endpoint: /apiv11/Mod/{mod_id}/DownloadPage
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming conventions.
                data:
                  download:
                    url: https://gamebanana.com/dl/example
                    filename: example.zip
                  checksum:
                    sha256: <hash>
                errors: []
            text/html:
              schema:
                type: string
                description: Raw response body.
              example: <!doctype html><html><body>Observed HTML payload from GameBanana endpoint.</body></html>
        '400':
          description: HTTP 400 response as observed for this endpoint.
        '401':
          description: HTTP 401 response as observed for this endpoint.
        '403':
          description: HTTP 403 response as observed for this endpoint.
        '404':
          description: HTTP 404 response as observed for this endpoint.
        default:
          description: Unexpected error, invalid input, permission issue, or upstream failure.
      tags:
      - 'v11: Mod'
      x-last-test:
        timestamp: '2026-04-19T17:42:02+00:00'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv11/Mod/1/DownloadPage
        contentType: text/html; charset=UTF-8
      x-curation-note: 'uncertain: probe content-type is not JSON'
      x-request-example: GET https://gamebanana.com/apiv11/Mod/1/DownloadPage
      x-observed-response-format: mixed
      x-observed-response-note: 'Observed format drift: previous docs had `html`, full live verification
        on 2026-06-01T21:22:07Z returned `json` (application/json; charset=UTF-8).'
