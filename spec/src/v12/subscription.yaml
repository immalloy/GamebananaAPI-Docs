paths:
  /apiv12/Subscription/Index:
    get:
      operationId: v12_subscription_index_get
      summary: List primary resources (Subscription)
      description: 'v12 `Subscription` endpoint `Index`. Purpose: Retrieve the default
        listing for this domain, usually used to populate landing pages or initial
        browse views. Expected input: No parameters are required for a baseline request.
        Returned data: typically a domain-specific JSON object/collection used by
        GameBanana web clients and integrations. Last verified probe content-type:
        `application/json; charset=UTF-8`.'
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv12/Subscription/Index`. Field
                  presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv12/Subscription/Index
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  items:
                  - id: 1
                    name: index-example-1
                  - id: 2
                    name: index-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v12: Subscription'
      x-last-test:
        timestamp: '2026-06-01T20:31:47Z'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv12/Subscription/Index
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `Index` responses to drive list views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv12/Subscription/Index
      x-observed-response-format: json
  /apiv12/Subscription/ListFilterConfig:
    get:
      operationId: v12_subscription_list_filter_config_get
      summary: Get list filter configuration (Subscription)
      description: 'v12 `Subscription` endpoint `List Filter Config`. Purpose: Return
        available filter/sort metadata used to construct list and search requests.
        Expected input: No parameters are required for a baseline request. Returned
        data: typically a domain-specific JSON object/collection used by GameBanana
        web clients and integrations. Last verified probe content-type: `application/json;
        charset=UTF-8`.'
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv12/Subscription/ListFilterConfig`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv12/Subscription/ListFilterConfig
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  items:
                  - id: 1
                    name: listfilterconfig-example-1
                  - id: 2
                    name: listfilterconfig-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v12: Subscription'
      x-last-test:
        timestamp: '2026-06-01T20:31:47Z'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv12/Subscription/ListFilterConfig
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `ListFilterConfig` responses to drive list views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv12/Subscription/ListFilterConfig
      x-observed-response-format: json
  /apiv12/Subscription/{id}/Config:
    get:
      operationId: v12_subscription_id_config_get
      summary: Get resource configuration (Subscription)
      description: 'v12 `Subscription` endpoint `{id} / Config`. Purpose: Return configuration
        data required by client workflows for this resource. Expected input: Provide
        required path identifiers and optional query filters as documented below.
        Returned data: typically a domain-specific JSON object/collection used by
        GameBanana web clients and integrations. Last verified probe content-type:
        `application/json; charset=UTF-8`.'
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
        description: Required path parameter `id`. Numeric identifier for the target
          entity. Use an ID from index/search endpoints or previously resolved links.
        example: 1
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv12/Subscription/{id}/Config`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv12/Subscription/{id}/Config
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  filters:
                  - key: sort
                    values:
                    - newest
                    - popular
                  defaults:
                    sort: newest
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v12: Subscription'
      x-last-test:
        timestamp: '2026-06-01T20:31:47Z'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv12/Subscription/1/Config
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `Config` responses to drive config views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv12/Subscription/1/Config
      x-observed-response-format: json
  /apiv12/Subscription/{id}/ProfilePage:
    get:
      operationId: v12_subscription_id_profile_page_get
      summary: Fetch profile page payload (Subscription)
      description: 'v12 `Subscription` endpoint `{id} / Profile Page`. Purpose: Return
        the full profile-page data model used by GameBanana clients for a single entity.
        Expected input: Provide required path identifiers and optional query filters
        as documented below. Returned data: typically a domain-specific JSON object/collection
        used by GameBanana web clients and integrations. Last verified probe content-type:
        `application/json; charset=UTF-8`.'
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
        description: Required path parameter `id`. Numeric identifier for the target
          entity. Use an ID from index/search endpoints or previously resolved links.
        example: 1
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv12/Subscription/{id}/ProfilePage`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv12/Subscription/{id}/ProfilePage
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  profile:
                    id: 1
                    name: Example
                    summary: Example profile payload
                  stats:
                    views: 123
                    likes: 10
                errors: []
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v12: Subscription'
      x-last-test:
        timestamp: '2026-06-01T20:31:47Z'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv12/Subscription/1/ProfilePage
        contentType: application/json; charset=UTF-8
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `ProfilePage` responses to drive profile views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv12/Subscription/1/ProfilePage
      x-observed-response-format: json
