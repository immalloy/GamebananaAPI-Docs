paths:
  /apiv12/Spray/{id}/ProfilePage:
    get:
      operationId: v12_spray_id_profile_page_get
      summary: Fetch profile page payload (Spray)
      description: 'v12 `Spray` endpoint `{id} / Profile Page`. Purpose: Return the full
        profile-page data model used by GameBanana clients for a single entity. Expected input: Provide required path identifiers and optional query filters as documented below. Returned data: typically
        a domain-specific JSON object/collection used by GameBanana web clients and
        integrations. Probe evidence indicates this route can sometimes return non-JSON
        content; implement content-type checks before parsing. Last verified probe
        content-type: `text/html; charset=UTF-8`.'
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
        description: Required path parameter id. Numeric identifier for the target entity. Use an ID from index/search endpoints or previously resolved links.
        example: 1
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients. Probe data observed `text/html` responses on this route; clients
            should branch parsing by content-type.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv12/Spray/{id}/ProfilePage`. Field
                  presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv12/Spray/{id}/ProfilePage
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  profile:
                    id: 1
                    name: Example
                    summary: Example profile payload
                  stats:
                    views: 123
                    likes: 10
                errors: []
            text/html:
              schema:
                type: string
                description: HTML response body observed for `/apiv12/Spray/{id}/ProfilePage`
                  in probe data.
              example: <!doctype html><html><body>Observed HTML payload from GameBanana
                endpoint.</body></html>
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v12: Spray'
      x-last-test:
        timestamp: '2026-06-01T20:43:35Z'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv12/Spray/1/ProfilePage
        contentType: text/html; charset=UTF-8
      x-curation-note: 'uncertain: probe content-type is not JSON'
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `ProfilePage` responses to drive profile views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv12/Spray/{id}/ProfilePage
      x-observed-response-format: mixed
      x-observed-response-note: 'Observed format drift: previous docs had `html`,
        full live verification on 2026-06-01T21:22:07Z returned `json` (application/json;
        charset=UTF-8).'
      x-live-verification-status: error
      x-live-verification-note: 'Full verification on 2026-06-01T21:22:07Z returned
        error: HTTP Error 404: Not Found'
  /apiv12/Spray/Index:
    get:
      operationId: v12_spray_index_get
      summary: List primary resources (Spray)
      description: 'v12 `Spray` endpoint `Index`. Purpose: Retrieve the default listing
        for this domain, usually used to populate landing pages or initial browse
        views. Expected input: No parameters are required for a baseline request.
        Returned data: typically a domain-specific JSON object/collection used by
        GameBanana web clients and integrations. Probe evidence indicates this route
        can sometimes return non-JSON content; implement content-type checks before
        parsing. Last verified probe content-type: `text/html; charset=UTF-8`.'
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients. Probe data observed `text/html` responses on this route; clients
            should branch parsing by content-type.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv12/Spray/Index`. Field presence
                  can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv12/Spray/Index
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  items:
                  - id: 1
                    name: index-example-1
                  - id: 2
                    name: index-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
            text/html:
              schema:
                type: string
                description: HTML response body observed for `/apiv12/Spray/Index`
                  in probe data.
              example: <!doctype html><html><body>Observed HTML payload from GameBanana
                endpoint.</body></html>
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v12: Spray'
      x-last-test:
        timestamp: '2026-06-01T20:43:35Z'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv12/Spray/Index
        contentType: text/html; charset=UTF-8
      x-curation-note: 'uncertain: probe content-type is not JSON'
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `Index` responses to drive list views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv12/Spray/Index
      x-observed-response-format: mixed
      x-observed-response-note: 'Observed format drift: previous docs had `html`,
        full live verification on 2026-06-01T21:22:07Z returned `json` (application/json;
        charset=UTF-8).'
  /apiv12/Spray/ListFilterConfig:
    get:
      operationId: v12_spray_list_filter_config_get
      summary: Get list filter configuration (Spray)
      description: 'v12 `Spray` endpoint `List Filter Config`. Purpose: Return available
        filter/sort metadata used to construct list and search requests. Expected
        input: No parameters are required for a baseline request. Returned data: typically
        a domain-specific JSON object/collection used by GameBanana web clients and
        integrations. Probe evidence indicates this route can sometimes return non-JSON
        content; implement content-type checks before parsing. Last verified probe
        content-type: `text/html; charset=UTF-8`.'
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients. Probe data observed `text/html` responses on this route; clients
            should branch parsing by content-type.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv12/Spray/ListFilterConfig`.
                  Field presence can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv12/Spray/ListFilterConfig
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  items:
                  - id: 1
                    name: listfilterconfig-example-1
                  - id: 2
                    name: listfilterconfig-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
            text/html:
              schema:
                type: string
                description: HTML response body observed for `/apiv12/Spray/ListFilterConfig`
                  in probe data.
              example: <!doctype html><html><body>Observed HTML payload from GameBanana
                endpoint.</body></html>
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v12: Spray'
      x-last-test:
        timestamp: '2026-06-01T20:43:35Z'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv12/Spray/ListFilterConfig
        contentType: text/html; charset=UTF-8
      x-curation-note: 'uncertain: probe content-type is not JSON'
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `ListFilterConfig` responses to drive list views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv12/Spray/ListFilterConfig
      x-observed-response-format: html
  /apiv12/Spray/Search:
    get:
      operationId: v12_spray_search_get
      summary: Search resources (Spray)
      description: 'v12 `Spray` endpoint `Search`. Purpose: Execute a server-side
        search query for this domain and return matching records. Expected input:
        No parameters are required for a baseline request. Returned data: typically
        a domain-specific JSON object/collection used by GameBanana web clients and
        integrations. Probe evidence indicates this route can sometimes return non-JSON
        content; implement content-type checks before parsing. Last verified probe
        content-type: `text/html; charset=UTF-8`.'
      responses:
        '200':
          description: Successful response. Payload structure depends on the endpoint
            intent and may include nested objects, arrays, and metadata used by GameBanana
            clients. Probe data observed `text/html` responses on this route; clients
            should branch parsing by content-type.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Response schema for `/apiv12/Spray/Search`. Field presence
                  can vary by version, resource state, and caller context.
              example:
                _meta:
                  endpoint: /apiv12/Spray/Search
                  method: GET
                  status: '200'
                  content_type_hint: application/json
                  note: Example payload based on observed endpoint behavior and naming
                    conventions.
                data:
                  items:
                  - id: 1
                    name: search-example-1
                  - id: 2
                    name: search-example-2
                  pagination:
                    page: 1
                    per_page: 15
                    total: 2
                errors: []
            text/html:
              schema:
                type: string
                description: HTML response body observed for `/apiv12/Spray/Search`
                  in probe data.
              example: <!doctype html><html><body>Observed HTML payload from GameBanana
                endpoint.</body></html>
        default:
          description: Fallback response for unexpected server behavior, invalid inputs,
            permission limitations, or upstream failures.
      tags:
      - 'v12: Spray'
      x-last-test:
        timestamp: '2026-06-01T20:43:35Z'
        status: 200
        result: ok
        url: https://gamebanana.com/apiv12/Spray/Search
        contentType: text/html; charset=UTF-8
      x-curation-note: 'uncertain: probe content-type is not JSON'
      x-developer-notes:
      - Implement retry with backoff and request timeouts; host behavior can differ
        between gamebanana.com and api subdomains.
      - Avoid strict schema locking; additive fields may appear without version bumps.
      x-common-use-cases:
      - Use `Search` responses to drive list views in client apps.
      - Preload this endpoint during initial page/app hydration when latency matters.
      x-edge-cases:
      - Response shape can change with permissions, moderation state, or hidden/deleted
        entities.
      - Treat empty arrays/objects as valid outcomes, not parser errors.
      x-request-example: GET https://gamebanana.com/apiv12/Spray/Search
      x-observed-response-format: mixed
      x-observed-response-note: 'Observed format drift: previous docs had `html`,
        full live verification on 2026-06-01T21:22:07Z returned `json` (application/json;
        charset=UTF-8).'
      x-live-verification-status: error
      x-live-verification-note: 'Full verification on 2026-06-01T21:22:07Z returned
        error: HTTP Error 404: Not Found'
